/*
 * metodos para selecionar elementos no DOOM
 * 
 * document.querySelector() retorna o primeiro elemento passado como argumento, seja pela tag, class ou id
 * document.querySelectorAll() retorna todos os elementos que foram passados como argumento
 * 
 * document.getElementById() seleciona um elemento pelo seu id [seletor CSS]
 *document.getElementByClassName() seleciona elementos pela sua classe [seletor CSS]
 * 
 */

